# SpectraScribe-
An autonomous-with-checkpoints agent that reads scientific papers, detects figures and tables, digitizes spectra (semi-automated curve extraction with a computed confidence score, not blind automation), and exports clean, ML-ready spectral data.
